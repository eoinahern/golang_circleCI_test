machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t eoinahern/hello_world_repo/
    - docker run -p 8080:8080 eoinahern/hello_world_repo/

deployment:
  hub:
    branch: master
    commands:
      - docker login -e eoinpahern@yahoo.co.uk -u eoinahern -p hellothere123
      - docker push eoinahern/hello_world_repo/
